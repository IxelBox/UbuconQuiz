using System;
using System.IO;
using UbuconQuiz;
//using NUnit.Framework;

namespace UbuconQuizTests
{
/*
	[TestFixture()]
	public class DatabaseFixture
	{
		//[Test()]
		public void CreateDB ()
		{
			string path = Path.Combine (Environment.CurrentDirectory, "Questions.db");
			var db = new Database (path);
			db.CreateDB ();
			
			//Table<Question> Question = db.
		}

		[Test()]
		public void CheckForDB ()
		{
			string path = Path.Combine (Environment.CurrentDirectory, "Questions.db");
			var db = new Database (path);
			db.RemoveDB ();
			Assert.IsFalse (db.CheckForDB ());
			db.CreateDB ();
			Assert.IsTrue (db.CheckForDB ());
			db.ResetDB ();
			Assert.IsTrue (db.CheckForDB ());
		}
	}
	*/
}

